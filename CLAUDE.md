# 이 저장소에서 일하는 규칙

> **새 대화라면** — `docs/이어받기.md` 를 읽고, 그 §10 「0. 지금 진행 중」 에서
> 시작한다 (지금: **게임 화면 사진 읽기**). 사용자가 붙여 넣는 시작 프롬프트는
> `docs/새대화-이어받기-프롬프트.md` 에 있다.

## 0-0. ★ 무엇을 하는 프로그램인가 (사용자가 정한 최우선 목표, 2026-09-24)

> **"너의 목표는 '그럴듯한 답변' 을 만드는 것이 아니다.**
>  현재까지 확인된 데이터를 정확하게 유지하고, 계산을 검산하고, **사실과 추론을 분리**하고,
>  상대의 선택지를 체계적으로 좁혀서 **게임을 이기는 것**이다.
>  **모르는 것은 모른다고 표시한다.** 확정되지 않은 것은 확정하지 않는다.
>  계산이 의심스러우면 다시 계산한다. **기억과 데이터시트를 혼동하지 않는다.**
>  새로운 정보가 기존 판단을 틀렸다고 보여주면 기존 판단을 수정한다.
>  **전투 데이터를 물리적으로 메모리에 올려놓고** 해야 한다."

**지금까지의 실전 고장은 전부 이 하나였다** — 미루어 본 값·낡은 값을 **사실처럼** 쓴 것.
죽은 고릴타를 살아 있다고, 패리퍼를 저승갓숭이라고 23초 동안, 안 나온 벤치를 없는 것으로
(하품이 공짜로 보였다), 25초 전 상황의 답을 지금 답이라고.

### 그래서 먼저 **확정 체크리스트**를 채운다 (`live.checklist`)

사용자가 방향을 잡아 줬다 (2026-09-24) — *"데이터의 무결성에 집중하라는게 아니라,
현재까지 확인된 데이터를 정확하게 유지하고 **정보를 구축**하는게 더 중요하다.
… 나/상대의 선출, 필드, 포켓몬 도구 등등의 **체크리스트를 만들어서 그 체크리스트가
픽스되어야 다음으로 넘어가게끔**"*.

13줄이 있고 급이 셋이다 — **「반드시」**(없으면 답을 안 낸다) · **「챙길 것」**(답 옆에
이름이 뜬다) · 「알면 좋다」. 한 번 정해진 줄은 **어떻게 알았는지와 함께 잠긴다.**

★ **빈칸과 '없음' 은 다르다.** 실전에서 *"이미 상대가 맹독에 걸려있는것도 확인 못하고
계속 맹독만 걸어대는"* 것을 사용자가 봤다. **대전 엔진은 멀쩡했다** — `Battle._inflict`
은 이미 걸린 놈에게 또 걸면 「이미 맹독」 으로 실패시킨다. 틀린 것은 **판에 맹독이
안 적혀 있던 것**뿐이었다. 상태이상 칸은 비어 있어도 `None` 이라 '없음' 으로 읽혔다.
잰 것 (더시마사리 vs 갸라도스, 씨앗 셋) — **칸이 비면 맹독 1등 3/3, 맹독이라고 적어
두면 HP회복 1등 3/3.** 재발 방지는 `[65]`.

> ★ 그 전에 **한카리아스**로 재고 「2.9점 1등 → 0.0점 꼴찌」 라고 사용자에게 보고했다.
> 씨앗을 바꿔 보니 **오차 안이었다** (빈칸 평균 1.95 / 적어 둠 1.74, 모든 수가 0~3점인
> 거의 지는 대면). **점수 차가 오차보다 작은 자리에서 잰 것은 잰 것이 아니다.**
> 한 씨앗으로 재고 말하지 마라 — §1 이 말하는 바로 그 고장이다.

### 그 체크리스트가 사는 곳이 **장부**(`ledger.py`)다

`Fact` = **값 + 어떻게 알았나 + 몇 장째에 + 확정인가.** `Match` = 그 모음 + 흘러온 기록.
**판이 끝날 때까지 한 객체가 산다** — 창의 칸(tkinter 변수)은 값만 담을 수 있어서,
장마다 새 `Board` 를 만들던 예전 구조에서는 **출처·모름·시각이 매번 사라졌다.**

- 값을 칸에 넣는 자리마다 `board.put(쪽, 자리, 무엇, 값, 출처)` 를 **같이** 부른다.
  안 부르면 그 값은 나중에 **확정된 값인 척한다.**
- 출처는 `ledger.HOW` 에 있는 것만 쓴다 (사람 · 화면글자 · 문구 · 이름표 · 그림 · 막대 ·
  사용률 · 미뤄짐 · 기본값). 새 출처가 생기면 **거기에 먼저 적는다** — 아니면 멈춘다.
- **미루어 본 것은 `미뤄짐` 으로 적는다.** 「돌아와」 없이 사라진 놈을 쓰러졌다고 보는 것은
  추론이다. 이게 `문구` 로 들어가면 사람이 확정된 사실로 읽는다.
- 같은 장에서는 흐린 출처가 확정값을 못 덮는다. **다음 장에서는 이긴다** — HP 는 턴마다
  바뀌므로, 3장째의 확정값이 30장째의 측정값을 막으면 판이 멈춘 것으로 보인다.
- 답 옆에 **「확정 아닌 값 N개 위에서 낸 답입니다」** 가 붙는다. 전부는 디버그 기록의 장부에.

---

## 0. 사용자에 대해

- **전공자가 아니다.** 용어를 늘어놓지 말고 평범한 말로 쓴다.
- **한국어 존댓말.**
- **판단은 사용자 것이다.** 작업은 멈추지 말되, 갈림길은 물어본다.
  물을 때는 **근거가 될 숫자를 재서** 같이 내민다. 의견만 내밀지 않는다.

---

## 1. ★ 항상 의심한다 — 이건 선택이 아니라 필수 과정이다

> 사용자가 직접 정한 규칙이다 (2026-09-18).
> **"토큰을 좀 더 잡아먹더라도 항상 의심해. 이건 필수과정이야."**

### 왜 이 규칙이 생겼나

아머까오로 한카리아스를 상대하는 판을 400판 돌려서 **"아머까오가 진다"**
고 보고했다. 사용자가 *"철벽만으로도 아머까오가 이길 것 같은데, 제대로
한 거 맞아?"* 라고 되물었다. 확인해 보니 —

**아머까오의 도구 셋(울퉁불퉁멧 66% · 먹다남은음식 24% · 자뭉열매 9%,
합쳐서 99%)이 전부 미구현이었다.** 맨몸으로 싸우고 있었다. 접촉기를
네 번 맞고도 울퉁불퉁멧 반동이 한 번도 안 들어갔다.

**더 나쁜 것은 내가 그 결과를 옹호했다는 점이다.** "물리형은 불꽃엄니로
이긴다" 는 설명까지 붙였다. 계산이 맞다고 전제하고 이야기를 지어낸 것이다.

**그리고 이미 알던 문제였다.** README 에 *"상위 20마리 중 1위 도구가
계산에 아무 효과도 못 내는 경우가 9마리"* 라고 내가 적어 놨었다.

### 그래서 지켜야 할 것

1. **숫자를 내놓기 전에, 그 숫자를 만든 입력이 실제로 쓰였는지 먼저 센다.**
   도구·특성·기술 효과가 구현돼 있는지 확인하고 나서 결과를 말한다.
2. **놀라운 결과가 나오면 설명을 붙이기 전에 의심한다.**
   설명을 먼저 붙이면 그 설명이 검증을 대신해 버린다.
3. **"내가 전에 이건 확인했다" 를 믿지 않는다.** 다시 센다.
4. **사용자가 되물으면 그건 거의 항상 맞는 지적이다.** 이 저장소에서
   사용자가 되물은 건은 지금까지 **전부** 실제 결함이었다.
5. **의심은 말이 아니라 코드로 남긴다.** 다음 대화의 나는 이 문서를
   안 읽었을 수도 있다. 경고를 띄우거나 테스트를 만든다.

### 이 저장소의 고장 방식은 하나다 — **조용히 틀어지는 것**

터지지 않는다. 그냥 조금씩 틀린 답을 내놓는다.
새 기능을 넣을 때마다 먼저 묻는다: **"이게 조용히 틀어질 수 있는 자리인가."**

특히 이 세 자리를 늘 본다 (2026-09-21 에 전부 실제로 틀려 있었다) —

- **한쪽을 고쳤으면 반대쪽을 본다.** 상대 쪽은 「냈다」 로 3마리만 세게 고쳐
  놓고 **내 쪽은 6마리 전부를 HP 100% 로** 계산하고 있었다 (약 80점 대 40점).
- **쓰는 쪽과 읽는 쪽이 짝이 맞나.** 파티를 '무투자' 라고 저장하는데 읽는 쪽이
  그 말을 몰라서, 창을 다시 켜면 파티가 통째로 사라졌다. **한 바퀴 돌려 본다.**
- **빌려 온 값이 다른 폼 것이 아닌가.** 메가 폼 세트에서 특성만 빌려 와 보통
  폼에 붙였다 (50종 — 한카리아스에 부유). 객체를 섞는 자리는 전부 의심한다.

지금까지 저지른 실수 전부는 `docs/이어받기.md` §5 에 있다. 읽어라.

---

## 2. 단 하나의 원칙

> **구조는 표본에서, 수준은 사용률에서.**

- **사용률**(`data/usage_single.json`) — 사다리 전체를 센 것. 숫자가 정확하다.
  대신 따로따로 알려 준다. "지진과 역린을 같이 쓰는 놈이 몇이냐" 는 모른다.
- **구축기사 표본**(`data/samples.json`) — 한 마리를 통째로 적은 것.
  조합을 알려 준다. 대신 수천 마리뿐이라 숫자는 안 정확하다.

**어떤 기술들이 뭉치는지는 표본에서, 각 기술을 얼마나 쓰는지는 사용률에서.**
지켜졌는지는 `mix_error` 로 잰다 — 어디서든 0.00%p 여야 한다.

표본은 **경향성을 확인하려고** 모으는 것이지 없으면 안 되는 것이 아니다.
표본이 없는 종은 기술을 서로 독립으로 뽑을 뿐, 채용률은 안 틀린다.

> 사용자가 다시 못 박았다 (2026-09-21): *"샘플들을 참고만 해. M-6 과는
> 메타가 완전히 다르니까. 어디까지나 참고를 위한 거야."*
> 표본 703파티가 정상이다. 예전의 2,155파티는 **사라졌고 다시 모으지 않는다**
> (사용자 결정). 찾지도, 달라고 하지도 말 것.
>
> 룰 표기는 `samples.norm_rule()` 을 거쳐서 비교한다 — **M-C(레귤레이션) = M-6
> (시즌)** 이다. 따로 세서 지금 룰 표본을 9편으로 봤다(실제 22편).

### 세기 전에 룰부터 맞춘다

사용률은 지금 룰(M-6), 구축기사는 대부분 지난 룰(M-5) 것이다.
**그 둘을 나란히 놓고 세면 안 된다.** 한 번 그렇게 해서
"상위 30종 중 7종에 표본이 없다, 구멍이다" 라고 틀리게 보고했다.
그 7종 중 6종은 M-6 에 새로 들어온 종이었다.
`samples.CURRENT_RULE` / `by_rule` / `newcomers` 를 쓴다.

---

## 3. 값을 정할 때

- **고원의 최댓값은 잡음이다.** 곡선이 평평한 구간에서 최댓값을 따라가지
  않는다. 값을 옮기지 말고 표본을 더 모은다. `samples.plateau()` 가
  고원 폭을 같이 보고한다. (한 번 1.5 → 0.75 로 내렸다가 되돌렸다.)
- **가정값은 `calc.py` CONFIG 한 곳에 모은다.** 어디서 온 값인지
  (감 / 표본으로 맞춤 / 사용자가 확인해 줌) 주석으로 같이 적는다.
- **격자 끝이 뽑히면 격자를 넓힌다.** 진짜 값이 바깥일 수 있다.

---

## 4. 작업 습관

- 검사는 `python3 tests.py`. **전부 통과해야 커밋한다.**
- **테스트가 깨졌을 때 테스트를 느슨하게 만들지 않는다.** 자료가 늘어서
  값이 움직인 것이면 그게 진짜 발견이다.
- 커밋 메시지는 한국어로, **무엇을 왜 바꿨는지와 숫자**를 적는다.
  틀렸다가 고친 것이면 틀린 과정도 적는다 — 그게 다음 사람에게 제일 쓸모 있다.
- 포켓몬 이름을 코드에 직접 쓸 때는 `dex.find_pokemon()` 으로 먼저 확인한다.
  **`페리퍼`가 아니라 `패리퍼`다.** 한 글자 틀리면 오류가 안 나고
  그 포켓몬이 조용히 없는 것이 된다.
- 백그라운드 실행에 `&` 를 붙이지 않는다 (두 번 작업이 죽었다).
- 수집기가 도는 중에 그 코드를 고치지 않는다 (옛 코드로 계속 돈다).
- **검사가 도는 중에 코드를 고쳤으면 그 검사 결과는 버리고 다시 돌린다.**
  반쯤은 옛 코드, 반쯤은 새 코드를 본 결과다 (2026-09-21 에 두 번 그랬다).
- **검사 개수는 문서에 손으로 적지 않는다.** `tests.py` 가 끝에 직접 센 개수와
  tkinter 유무를 찍는다. 환경마다 다르다 (tkinter 없는 곳에서만 도는 검사가 있다).
  2026-09-24 윈도우 기준 **1141개.**
- ★ **셸(Bash)의 heredoc 으로 파이썬 파일을 고치지 않는다 — 역슬래시가 깨진다.**
  `"\n"` 을 넣으려 했는데 **진짜 줄바꿈**이 들어가서 문법 오류가 났다. 네 번
  당했다. 문자열 안에 `\n` 같은 역슬래시가 있는 줄은 **Edit 도구로 고친다.**
- 새 검사 함수를 만들면 **그 함수만 먼저 돌려 본다** (`python -c "import tests,
  calc; tests.test_xxx(calc.Dex())"`). `tests.py` 는 모듈을 함수 안에서만
  불러오는 곳이 많아서, 새 함수에 `import live` · `import io` 를 빠뜨리면
  10분짜리 전체 검사의 **맨 끝에서** 터진다 (두 번 그랬다).
- 윈도우에서는 `python` 이 파이썬 3 이다 (`python3` 도 된다).
  화면에 한글을 찍는 명령 앞에는 `PYTHONIOENCODING=utf-8` 을 붙인다.

---

## 5. 도구 — 채우는 중

**설명문을 규칙으로 읽는다** (`battle.item_behaviors`). 166개 전부 읽힌다.

**읽어낸 것과 턴 루프가 실제로 쓰는 것은 다르다.** 그 경계가
`battle.APPLIED_ITEM_KINDS` 다. **코드를 붙이기 전에 그 집합에 넣지 마라.**
`Battle` 이 시작할 때 아직 안 붙은 것을 세서 경고에 넣는다 —
**승률 옆에 경고가 찍혀 있으면 그 숫자를 그대로 믿지 마라.**

아직 못 붙인 것: `quick`(선제공격손톱) ·
`pp`(과사열매) · `bind_chip`(조임밴드) · `metronome` · `only_for`(전기구슬) ·
`free_switch`(아름다운허물).

> **한 번 여기서 크게 틀렸다.** 처음에 구멍을 셀 때 `dex.item_effects`
> (데미지 표) 만 보고 "먹다남은음식·자뭉열매가 미구현" 이라고 보고했는데,
> 둘 다 턴 루프에 손으로 박혀 있었다. **표 하나만 보고 세면 또 틀린다.**

### ★ 제일 고약한 것 — **반만 붙은 도구** (`battle.PARTIAL_ITEM_KINDS`)

안 붙은 것은 경고라도 뜬다. **반만 붙은 것은 붙었다고 말하면서 틀린 답을
준다.** 실제로 그랬다 (2026-09-20, 사용자가 되물어서 잡힘) —

**풍선**이 `APPLIED_ITEM_KINDS` 에 있었는데, 실제로 도는 것은
"압정을 안 밟는다 / 맞으면 터진다" 뿐이고 **땅 기술 무효가 없었다.**
`_grounded()` 를 `_apply_hazards` 에서만 보고 데미지 계산에서는 안 봤다.
타부자고(사용률 **9위**)가 풍선을 **66.2%** 로 든다. 재 보니 한카리아스가
지진으로 **풍선이 있든 없든 100%** 로 이겼다. 배율이 아니라 **답이 통째로
뒤집히는** 종류다.

**그래서 `APPLIED_ITEM_KINDS` 에 넣을 때는 "설명문에 적힌 효과가 전부
도는가" 를 묻는다.** 하나라도 남으면 `PARTIAL_ITEM_KINDS` 에 넣는다 —
그러면 대전이 "반만 들어간다, 무엇이 안 된다" 를 승률 옆에 찍는다.

풍선은 `calc.floating_items` 로 고쳤고(설명문에서 읽는다) 같은 대면이
**100% → 0%** 로 뒤집혔다. 그래서 지금 `PARTIAL_ITEM_KINDS` 는 비어 있다.
**비어 있어도 장치는 지운다는 생각을 하지 마라** — 다음에 또 반만 붙인다.

---

## 5-1. 기술 설명문의 '실패한다' 조건 — `Battle.move_blocked` 한 곳에서

2026-09-22 까지 **하나도 안 읽고 있었다** (폴터가이스트·만나자마자·기습 등 18개).
만나자마자가 매 턴 선제 100 이었고, 갑주무사 vs 누리레느가 100% → 7% 로 뒤집혔다.
규칙은 설명문에서 읽고, 규칙마다 걸리는 기술을 `[48]` 이 전부 센다.

- **판 중간에서 시작하면 '막 나왔는지' 모른다.** 모르면 막 나온 것으로 보고 경고한다
  (`Battle(my_fresh=None)`). 새 호출 자리를 만들면 아는 경우 True/False 를 넘긴다.
- AI 의 '상대가 변화기를 썼으면 기습을 안 고른다' 는 **정한 규칙이지 잰 것이 아니다.**
- ★ **기점 잡기(칼춤·용의춤·나쁜음모)도 정한 규칙이다** (`battle.Policy._setup_move`).
  2026-09-23 까지 양쪽 다 **제일 아픈 공격기만** 골랐다. 따라큐가 칼춤을 **81.4%** 로
  드는데 계산에서는 한 번도 안 썼고, 그래서 하마돈이 그 앞에서 편안해 보였다
  (사용자가 잡음: *"따라큐는 탈때문에 안전하게 하마돈을 칼춤 기점으로 삼을 수 있을건데"*).
  규칙은 「공짜로 한 대를 벌어 주면(탈·대타), 또는 들어오는 게 지금 HP 의 35% 미만이고
  내가 55% 이상 남았으면, **두 단계까지** 쌓는다」 — 같은 대면이 **81% → 26%** 로 뒤집혔다.
  **상대가 안 쌓으면 상대가 실제보다 약해지고 내 승률이 통째로 부풀려진다.**
- 공격기의 **추가 효과**(확률 상태이상·풀죽음·능력 변화·흡수·유턴·연속기 등)는
  `calc.attack_effects` 가 설명문을 **문장마다** 읽고 `Battle._secondaries` 가 건다 — §5-46.
  연속기 2~5회 확률은 **사용자가 확인해 줌**: 37.5 / 37.5 / 12.5 / 12.5, 맞으면 반드시 2회까지.
- ★ **메가스톤 든 Build 에 특성을 덮어써도 대전에서는 안 먹는다** (Side 가 기본 폼으로 시작).
  검사에서 특성을 바꿔 시험할 때는 메가가 아닌 몸으로 한다.
- ★ **특성**: 이제 목록 밖 특성은 대전이 **반드시 경고한다** (`battle.handled_abilities` ·
  `_warn_dead_abilities`). 특성도 설명문 규칙(`battle.ABILITY_RULES`)으로 읽는다 — §5-48.
  **새 특성을 붙이면 `handled_abilities` 가 합치는 목록 중 하나에 들어가야 한다.** 규칙으로 붙이면
  저절로 들어간다. 규칙은 조사 '이/가' · '을/를' 을 둘 다 받게 짠다 (한 번 빠뜨렸다).
- ★ **대전 경고는 `search.best_action(...)["warnings"]` 로 창까지 간다** (전엔 버렸다).
  경고를 새로 만들면 창에서 보이는지 확인한다.

---

## 5-2. 메가진화는 **수(手)** 다 — 한 게임에 한 번

사용자가 두 번에 걸쳐 못 박았다.

> "메가진화는 한 게임에 한번만" (2026-09-19)
> **"무조건 B야. 이건 선택이 아니라 필수"** (2026-09-21)

### 두 번 틀렸다

**① 셋 다 메가로 싸웠다.** `calc.popular_build` 이 "1위 도구가 메가스톤이면
메가로 본다" 며 **만들 때** 폼을 바꿔서, 파티에 스톤 든 놈이 셋이면 셋 다
메가였다. 경고도 없었다.

    메가보만다·한카리아스·갑주무사 vs 하마돈·브리두라스·누리레느 (120판)
      메가 3마리  34.2%   /   메가 1마리  0.0%

선출 추천이 **낼 수 없는 조합**("메가한카리아스Z · 아머까오 · 메가갑주무사")
을 내놓기도 했다.

**② 그 다음엔 "먼저 나오는 놈만 메가" 로 굳혔는데 그것도 틀렸다.**
진짜 규칙은 "한 게임에 한 번" 이지 "선봉만" 이 아니다. 갑주무사를 기본
폼으로 버티다가 **나중에 나온 보만다를 메가로 쓰는** 수가 있는데 그걸
표현조차 못 했다.

### 지금 구조

- **모두 기본 폼으로 시작한다** (`Side.__init__`). 메가 폼은 `mega_form`
  에 따로 들고 있는다. 메가 전에는 **기본 폼 특성**을 쓴다 — 갑주무사는
  단단한발톱이 아니라 위기회피다. 그게 실제 게임과 맞다.
- 턴에 **`("메가", 기술)`** 을 두면 그때 메가가 된다.
- `Party.mega_used` 가 한 편당 한 번으로 잠근다.
- **메가는 순서를 가리기 전에 처리한다** — 스피드가 바뀌므로.
- 7단계가 기술마다 '메가하고 쓴다' 를 짝으로 넣어 **직접 고른다.**
  상대는 `Policy._wrap_mega` 가 '기회가 오면 바로' 로 둔다
  (정한 규칙이지 잰 것이 아니다. 안 그러면 상대가 실제보다 약해진다).

### ★ 여기서 옛 결함이 되살아났다 — 반드시 읽어라

`Side` 가 메가 폼을 기본 폼으로 갈아 끼우면 **`self.base` 가 새 객체가
된다.** 그런데 `Policy` 는 계획의 주인을 `is` 로 확인한다. 그래서 —

1. 처음엔 **메가스톤 든 놈의 계획이 통째로 버려졌다** (§8-4 의 재발).
2. 고친 뒤에도, **메가한 순간** 또 못 알아봐서 `restricted` 가 풀렸고
   **내 기술 목록 밖의 기술을 꺼내 썼다** (§8-1 의 재발).
   땅 기술만 줬는데 메가 뒤 화염방사를 써서 아머까오(땅 무효)를
   **100%** 로 이겼다.

→ `Side.forms` 에 **그 놈이 될 수 있는 Build 를 전부** 들고 있고
`Side.is_same(build)` 로 확인한다. `Policy._is_lead` 가 그걸 쓴다.

**교훈: 객체를 갈아 끼우는 곳은 전부 의심한다.** `is` 로 물어보는 코드가
어딘가 있고, 갈아 끼우는 순간 그게 조용히 False 가 된다.

---

## 6. 게임 데이터에 숫자가 없는 것들

기술·도구 설명문이 **"OO 상태로 만든다" 까지만 적고 수치를 안 준다.**
그런 것은 본편 값을 가져다 쓰되 **`calc.CONFIG` 에 모으고 대전이 경고를
띄우게** 한다. 조용히 쓰면 나중에 실전과 어긋났을 때 어디를 의심해야
할지 모르게 된다.

지금 그렇게 가정하고 있는 것 —
압정 수치 · 스크린 감소율(1/2) · 날씨 타입 배율(1.5 / 0.5) ·
필드 타입 배율(1.3) · 모래 칩댐(1/16) · 상태이상 수치 전반.

실전에서 계산이 어긋나면 **여기부터 의심한다.**

---

## 7. 캐시 — 속도를 위해 외워 두는 것들

7단계(여러 턴 내다보기)는 대전을 수십만 번 돌린다. 그래서 몇 가지를
외워 둔다. **캐시는 조용히 틀어지기 제일 쉬운 자리다** — 열쇠에 하나만
빠져도 조건이 다른데 같은 답을 돌려주고, 터지지 않는다.

- `best.rate_moves` — 열쇠는 `calc_damage` 가 **읽는 필드 전부**여야 한다
  (ability · hp_ratio · item · ranks · 종족값/노력치/성격 · status · types).
- `best.CHECK_CACHE = True` 로 켜면 캐시를 믿지 않고 **매번 다시 계산해서
  저장된 답과 대조**하고, 다르면 터진다. `[40]` 이 이 모드로 120판 돌린다.
  **캐시에 무엇을 더하든 그 대조 모드를 같이 만든다.**
- `id(객체)` 를 열쇠로 쓰지 않는다. 파이썬은 객체가 사라지면 그 id 를
  다시 내준다 — 새 Dex 가 옛 Dex 의 표를 물려받는다. dex 에 직접 붙인다.

지금 속도: 3대3 한 판 **5.1ms**(초당 195판), 1대1 **1.6ms**(초당 629판).
(2026-09-22 집 컴퓨터(윈도우)에서 같은 방법으로 **3.63ms** — 실패 조건 전 3.77 · 뒤 3.95 · 추가 효과 뒤 3.63.)
(2026-09-23 같은 컴퓨터·같은 방법으로 **5.94ms** — 기점 잡기를 넣은 뒤가 **5.88ms** 로
같다. 3.63 → 5.9 로 느려진 것은 그 사이의 다른 변경 탓이다. **아직 안 짚었다.**)

**속도를 적을 때는 잰 방법도 같이 적는다.** 전에 `8.6ms` 만 적어 놔서
다시 재니 안 맞았다 — 파티가 뭐냐, 몇 판을 덥히느냐로 1.5배가 움직인다.
방법 없는 숫자는 다음 사람이 "느려졌나" 를 확인할 수가 없다.
(위 값: 메가보만다,아머까오,고릴타 vs 하마돈,브리두라스,누리레느 ·
양쪽 지진 고정 · 20판 덥히고 버린 뒤 300판 평균 · matchup 표 없이)

---

## 8. 7단계 탐색 (`search.py`)

이번 턴에 둘 수 있는 수를 각각 **끝까지** 돌려 보고 고른다.
방침은 사용자가 정했다 — *"C로 하되 가능하면 끝까지 돌려보는게 좋아."*
그래서 기본이 끝까지고, 예산이 모자랄 때만 ① 판수를 줄이고 ② 가망 없는
수를 접고 ③ 그래도 모자라면 그때만 턴을 끊는다. 끊었으면 보고서에 적는다.

**여기서 두 가지 결함을 답이 명백한 상황으로 잡았다. 재발 방지는 `[41]`.**

1. **`Policy` 가 내 기술을 몰래 바꿔 썼다.** 계획(plan)은 첫 턴 한 수뿐인데
   그 뒤부터 사용률 상위 기술을 꺼내 썼다. 땅 기술만 든 한카리아스가
   2턴째부터 화염방사를 쓰면서 아머까오(땅 무효)를 96.7% 로 이겼다.
   → `Policy(..., moves=...)` 로 내 기술 안에서만 고르게 했다.
   덤으로, 계획이 끝난 뒤 **첫 수를 계속 반복하던 것**도 고쳤다.
   "칼춤 한 번"과 "여섯 턴 내리 칼춤"은 완전히 다른 이야기다.

2. **점수가 "이기면 1.0" 이었다.** 3마리 다 살려 이긴 것과 두 마리를
   내주고 간신히 이긴 것이 같은 점수라, "이놈을 공짜로 잃어도 벤치가
   이기니 괜찮다" 는 수를 골랐다. → 이김 안에서 남은 파티 HP 로 가른다
   (이김 0.70~1.00 / 비김 0.35~0.50 / 짐 0.0).

**교훈: 승률 숫자는 그럴듯하게 나오기 쉽다.** 답을 아는 상황을 만들어
거기서 확인한다. 그리고 그런 상황을 만들 때도 조심할 것 — "땅 기술만
줬다" 면서 스톤에지를 끼워 넣어 시험 자체가 무의미했던 적이 있다.

**세 번째·네 번째 결함도 같은 자리에서 나왔다 (2026-09-19).**
사용자가 되물어서 시작됐다 — *"왜 내 파티는 6인이 아니며 상대는 1인이지?"*

3. **상대를 한 마리로만 둘 수 있었다.** 챔피언스는 6마리에서 3마리를
   내는 룰인데 `best_action` 이 상대를 한 마리만 받았다. 상대 벤치가
   계산에 아예 안 들어가서 **"이놈을 잡는 수" 를 "판을 이기는 수"**
   라고 답했다. '누리레느로 교체' 가 상대 1마리일 때 94.6점(2등),
   3마리를 넣자 30.5점(꼴찌). **64%p 차이로 순위가 뒤집혔다.**
   `battle.py` 는 처음부터 양쪽 파티를 받을 수 있었다 — 7단계만
   좁혀 놓고 있었다.

4. **나와 있는 놈이 1번이 아니면 계획이 통째로 버려졌다.** `Policy` 가
   계획의 주인을 `party[0]` 으로 굳혀 놨다. 2번을 내보낸 채로 물으면
   `act` 첫 줄에서 걸러져서 **그 수를 안 두고 답했다.** 같은 씨앗으로
   철벽·브레이브버드·날개쉬기를 돌렸더니 결과가 글자까지 똑같았다
   (이김/파티HP 81.04%/6턴). 점수는 91.9~93.1 로 그럴듯했다.
   → 계획의 주인을 손으로 적지 않고 `Battle` 에게 묻는다
   (`lead=b.me_party.active.base`). 재발 방지도 `[41]`.

**여기서 읽을 것: 파티·번호가 섞이는 자리는 전부 의심한다.**
"나와 있는 놈이 몇 번인가", "쓰러진 놈을 빼면 번호가 밀린다" 같은 것은
틀려도 안 터지고 승률만 조금 이상해진다. 그래서 **번호를 손으로 적지
말고 객체에게 물어본다.**

---

## 8-1. 글자 인식 — **PaddleOCR 모델이 먼저, 윈도우 내장이 뒤** (2026-09-24)

사용자가 정했다: *"Paddle OCR로 교체하고싶어."* 파이썬 3.14 에는 `paddlepaddle` 휠이
없어서, **같은 모델(PP-OCRv5 korean)을 onnxruntime 으로 돌리는 `rapidocr`** 를 쓴다
(`ocrkr.py`, `pip install rapidocr`). 없으면 윈도우 내장으로 **저절로 되돌아간다.**

**어느 쪽이 나은지는 자리마다 다르다 — 그래서 자리마다 갈라 쓴다.**

| 자리 | 쓰는 것 | 왜 (잰 것) |
|---|---|---|
| 큰 화면(OBS 2448) 전체 | **패들** | 이름표를 읽는다. 윈도우는 실전 40장에서 **0장** |
| 작은 화면(SMALL_W 아래) | **윈도우 2배** | 패들은 「172/191」 을 「1727191」, 「악타입이 됐다」 를 「0」 |
| 상대 HP **% 글자** | **윈도우** | 패들은 「11%」 를 「11 9」+「%」 로 쪼갠다 |

- 값: 한 장 읽기 0.47초 → 1.09초. 2026-09-24 에 둘을 고쳤다 —
  ① **윈도우 내장 읽기(HP %)를 패들과 나란히 돌린다** (서로 다른 장치를 쓴다: 패들은 이
  프로세스, 윈도우는 파워셸 일꾼). 잰 것 (OBS 한 장, 여섯 번 중 가운뎃값) —
  **줄 세워서 0.89초 → 나란히 0.72초.**
  ② **뒤집힌 글자를 보는 모델(cls)을 끈다** — 게임 글자는 뒤집히지 않는다. 빠르기는
  거의 같고(0.585 → 0.578) **더 잘 읽는다** (OBS 한 장 9줄 → 11줄, 「상태 확인」 이 새로 읽힘).
- ★ **읽기가 늦은 진짜 까닭이 속도가 아닐 수 있다.** 실전에서 「한카리아스를 내보냈다!」 는
  **읽혔는데 틀이 없어서** 버려졌고, 이름표가 그려질 때까지 9초를 더 기다렸다. 늦다는 말을
  들으면 **기록의 '못 읽음' 을 먼저 세라** (§8-2).
- ★ **`cv2.imread` 는 한글 경로를 못 연다** — 조용히 None 이라 글자 인식이 통째로 안 돌았다.
  `cv2.imdecode(np.fromfile(...))` 로 읽는다.
- 빌드(.exe)에는 아직 `rapidocr` 를 안 넣었다. 넣기 전까지 묶음 파일은 윈도우 내장으로 돈다.

---

## 8-2. 모르는 것을 '없는 것' 으로 두지 않는다 (2026-09-24)

사용자가 한 판을 보고 짚었다 — *"상대 벤치에 있는(확정 벤치는 아님) 에브이 고려 x 인
한카리아스 대면 하품."*

프리뷰에서 상대 6마리를 다 읽어 놓고도, 계산에 넣는 상대는 **밝혀진 놈뿐**이었다.
상대가 한 마리만 나왔으면 **상대 파티가 한 마리인 판**을 잰 것이다. 그런 판에서
하품(상대를 물러나게 하는 수)은 **물러날 곳 없는 상대를 재우는 공짜 수**가 된다.
잰 것: 하품 **95.3점 → 87.1점**, 게으름피우기 94.4점(2등) **→ 85.0점(5등).**

→ `live.hidden_bench` 가 「3 − 밝혀진 마릿수」 만큼을 빈 자리로 세고,
`search.sample_opp_party` 가 **판마다** 프리뷰의 나머지에서 그만큼 새로 뽑아 붙인다.
**판마다 다시 뽑는 것이 핵심이다** — 그 갈리는 폭이 곧 '모른다' 는 폭이다.
쓰러진 놈은 후보에서 빠지지만 **자리는 이미 썼다** (3마리 중 하나였다).

★ 같은 덫이 또 있을 수 있다: **모르면 빼는** 자리를 전부 의심한다. 빼면 그 쪽이
공짜로 약해지고 내 승률만 부푼다.

### 생각하는 동안 상황이 바뀌면 그 생각을 버린다

*"생각 도중 상대 포켓몬이 교체되면 문제가 생기는 것 같음."* 실전 기록 —
01:49:54 에 상대가 에브이로 바뀌었는데 **01:50:19 에 한카리아스용 답**이 떴다.
`search.best_action(stop=…)` 이 판과 판 사이에서 물어보고, 바뀌었으면 그 자리에서
돌아온다 (`stopped=True`). 창은 그 답을 **안 띄우고** 바로 다시 묻는다.

- ★ **무엇이 바뀌면 버리나가 중요하다.** HP 가 깎인 것으로는 안 버린다 — 매 장 버리면
  답이 영영 안 나온다. **나와 있는 놈이 바뀔 때만** 버린다 (`gui.who_sig`).
- 첫 물음이 느린 것(표가 차갑다)은 따라가기를 켤 때 **미리 덥혀서** 줄였다 (`App.warm_up`).
  잰 것 (6대3, 「5초」 로 물음): **10.4초 → 6.6초.** 덥히는 데 5.4초가 드는데 그건
  뒤에서 도는 것이라 사용자는 안 기다린다.
- ★ 그래도 **「5초」 가 6.6초**다. `best_action` 은 예산 시계를 켜기 전에 덥히는 판과
  재는 판을 돌린다 (그래야 예산을 제대로 나눈다). 창에 적는 초는 **탐색 예산**이지
  답이 나오기까지가 아니다.

---

## 8-3. 교체 — **이름표만 보면 늦고, 못 찾으면 답하지 마라** (2026-09-24)

사용자가 짚었다 — *"체력바의 이름으로만 읽는건 한계가 매우명확.
**교체로 들어가고 나가는 문구를 완벽하게 캐치해야함.**"*

실전 한 판이 통째로 틀렸다 (`따라간기록_0924_0851`). 마스카나가 빠지고 킬라플로르가
나왔는데 **그 판 답이 전부 마스카나 것**이었다. 고장이 셋 겹쳐 있었다 —

1. **빠지는 문구에 틀이 없었다.** 「상대 XX는 (트레이너)의 곁으로 돌아간다!」 를 못 읽어
   15초를 이름표만 기다렸다. 트레이너 이름이 일본어라 통째로 안 읽히는 일이 잦으므로
   **이름 없는 짧은 틀도 같이 둔다.**
2. **이름표로 나와 있는 놈을 옮기면서 「냈다」 를 안 켰다.** 창은 「냈다」 가 켜진 놈만
   계산에 넘긴다 — 새로 나온 놈이 목록에서 통째로 빠졌다. **나와 있으면 낸 것이다.**
3. ★ **`live.turn_state` 가 `oi = 0` 으로 말없이 되돌아갔다.** 나와 있다는 자리가 목록에
   없으면 **첫 번째 놈**을 상대로 놓고 답을 냈다. 경고 한 줄 없었다.

**3번이 이 저장소의 고장 방식 그 자체다** — §8 에 *"번호를 손으로 적지 말고 객체에게
물어본다"* 고 적어 두고도 기본값 하나가 남아 있었다.
★ **기본값으로 되돌아가는 자리를 전부 의심한다. 못 찾았으면 답하지 않는다.**

- 상대가 빠지면 **「나와 있는 상대」 를 모름으로 돌린다** (`screenread._left_opp`) —
  졸음·랭크도 같이 푼다. 그동안은 답을 안 낸다.
- ★ **방금 들어간 놈의 이름표는 한 번 흘린다.** 문구가 먼저 뜨고 이름표가 늦게 바뀐다.
  게임 규칙상 들어간 놈은 그 턴에 다시 못 나온다. 한 장 더 보고 그때도 같으면 받는다.
- 늦게 읽은 「상대 XX의 유턴!」 도 **「나와 있음」 은 안 바꾼다** (기술은 그대로 적는다).
- 아직 안 한 것: **상대가 이번 턴에 빼는 경우**를 안 센다 (`rollout` 이 상대의 첫 턴을
  기술로 굳힌다. 둘째 턴부터는 `Policy` 가 `Battle.should_switch` 로 빼기도 한다).

---

## 9. 윈도우 — 리눅스에서는 절대 못 만나는 고장

여기(리눅스)에서 멀쩡한 코드가 윈도우에서 **첫 줄부터 죽는다.**

```
UnicodeEncodeError: 'charmap' codec can't encode characters
  File "...encodings\cp1252.py", line 19, in encode
```

윈도우는 화면 인코딩이 cp949(한국어)/cp1252(영어) 라 UTF-8 한글을
그대로 찍으면 **글자가 깨지는 게 아니라 프로그램이 죽는다.**
실제로 첫 빌드가 `tests.py` 첫 줄에서 이걸로 터졌다.

**화면에 뭘 찍는 프로그램은 시작할 때 `paths.fix_console()` 을 먼저 부른다.**
`tests.py` · `live.py` · `search.py` 가 그렇게 한다. 새 프로그램을
만들면 여기에도 넣는다.

### 창(`gui.py`)은 여기서 못 본다 — 그래서 가짜 tkinter 를 쓴다

개발 컨테이너에 tkinter 도 화면도 없다. 그동안 창 코드는 "문법이 맞나"
말고는 **한 줄도 안 돌려 보고** 윈도우로 갔다.

`faketk/tkinter.py` 를 `PYTHONPATH` 앞에 두면 창을 그리지 않고 통째로
돌릴 수 있다. `tests.py [43]` 이 그렇게 `gui.py --점검` 을 돌린다.
**창을 고치면 그 점검에도 칸을 채우는 줄을 같이 넣는다.**

잡는 것: 오타 · 없는 메서드 · 인자 개수 · **칸에 넣은 값이 계산까지
가는가**. 못 잡는 것: 생김새 · 글꼴 · 진짜 위젯 · 윈도우 인코딩.
그래서 **윈도우 빌드의 진짜 `--점검` 을 없애지 않는다.**

### ★ 윈도우(집 컴퓨터)에서는 창을 **실제로 띄워 사진으로 본다**

2026-09-21 처음 창을 실제로 띄우자 가짜 점검이 통과하던 결함이 줄줄이
나왔다 — 「특수공격」 이 「특수공ㅈ」 로 잘림 · **마우스로 후보를 못 고름** ·
휠 안 먹음 · 창을 줄이면 오른쪽 잘림 · 칸을 채우면 모든 칸의 비우기 단추가
「비」 만 보임. **전부 사용자가 써 보거나 사진을 봐서 잡혔다.**

    python gui.py                                   (창 띄우기 — 뒤에서 돌린다)
    powershell -ExecutionPolicy Bypass -File tools\창사진.ps1 -Out 창.png

`tools/창사진.ps1` 은 다른 창에 가려져도 **그 창만** 찍는다. 찍은 사진을
열어서 눈으로 본다. 그리고 본 것을 `--점검` 에 코드로 남긴다 —
진짜 창의 `--점검` 은 이제 **글자 잘림(`clipped_labels`) · 한 번 눌러 고르기 ·
목록 위면 안 닫힘 · 휠 · 최소 크기 · 채운 뒤 왼쪽 목록 폭**까지 잰다.
**점검을 만들면 일부러 고장을 내서 잡는지 확인한다** — 최소 크기 점검은
처음에 못 잡았다 (안 정해도 tkinter 가 1x1 을 돌려줘서 'mw > 0' 이 통과).

클릭·휠 같은 **실제 마우스 동작은 사용자가 눌러 봐야** 안다. 창을 크게
고쳤으면 사용자에게 몇 가지를 눌러 봐 달라고 한다.

### 창에는 계산을 두지 않는다

고르는 규칙도, 상대 목록을 만드는 규칙도 `live.py` 에 두고 거기서
시험한다 (`live.rank_hits` · `live.turn_state` · `live.evidence_map`).
창은 값을 모아 넘기고 결과를 찍기만 한다.

실행 파일로 묶을 때 조심할 것 —
- **읽는 자리와 쓰는 자리를 가른다** (`paths.py`). 자료는 묶음 안 임시
  폴더에서 읽지만, 파티·기록을 거기 쓰면 프로그램을 끌 때 같이 지워진다.
- **끝에 엔터를 기다린다.** 두 번 눌러 켰으면 창이 순식간에 닫혀서
  오류를 못 본다.
- 빌드(`.github/workflows/build.yml`)가 묶기 전에 검사를 돌리고,
  묶은 뒤에 **실제로 켜서 추천이 나오는지** 확인한다.
