# 이 저장소에서 일하는 규칙

## 0. 사용자에 대해

- **전공자가 아니다.** 용어를 늘어놓지 말고 평범한 말로 쓴다.
- **한국어 존댓말.**
- **판단은 사용자 것이다.** 작업은 멈추지 말되, 갈림길은 물어본다.
  물을 때는 **근거가 될 숫자를 재서** 같이 내민다. 의견만 내밀지 않는다.

---

## 1. ★ 항상 의심한다 — 이건 선택이 아니라 필수 과정이다

> 사용자가 직접 정한 규칙이다 (2026-09-18).
> **"토큰을 좀 더 잡아먹더라도 항상 의심해. 이건 필수과정이야."**

### 왜 이 규칙이 생겼나

아머까오로 한카리아스를 상대하는 판을 400판 돌려서 **"아머까오가 진다"**
고 보고했다. 사용자가 *"철벽만으로도 아머까오가 이길 것 같은데, 제대로
한 거 맞아?"* 라고 되물었다. 확인해 보니 —

**아머까오의 도구 셋(울퉁불퉁멧 66% · 먹다남은음식 24% · 자뭉열매 9%,
합쳐서 99%)이 전부 미구현이었다.** 맨몸으로 싸우고 있었다. 접촉기를
네 번 맞고도 울퉁불퉁멧 반동이 한 번도 안 들어갔다.

**더 나쁜 것은 내가 그 결과를 옹호했다는 점이다.** "물리형은 불꽃엄니로
이긴다" 는 설명까지 붙였다. 계산이 맞다고 전제하고 이야기를 지어낸 것이다.

**그리고 이미 알던 문제였다.** README 에 *"상위 20마리 중 1위 도구가
계산에 아무 효과도 못 내는 경우가 9마리"* 라고 내가 적어 놨었다.

### 그래서 지켜야 할 것

1. **숫자를 내놓기 전에, 그 숫자를 만든 입력이 실제로 쓰였는지 먼저 센다.**
   도구·특성·기술 효과가 구현돼 있는지 확인하고 나서 결과를 말한다.
2. **놀라운 결과가 나오면 설명을 붙이기 전에 의심한다.**
   설명을 먼저 붙이면 그 설명이 검증을 대신해 버린다.
3. **"내가 전에 이건 확인했다" 를 믿지 않는다.** 다시 센다.
4. **사용자가 되물으면 그건 거의 항상 맞는 지적이다.** 이 저장소에서
   사용자가 되물은 건은 지금까지 **전부** 실제 결함이었다.
5. **의심은 말이 아니라 코드로 남긴다.** 다음 대화의 나는 이 문서를
   안 읽었을 수도 있다. 경고를 띄우거나 테스트를 만든다.

### 이 저장소의 고장 방식은 하나다 — **조용히 틀어지는 것**

터지지 않는다. 그냥 조금씩 틀린 답을 내놓는다.
새 기능을 넣을 때마다 먼저 묻는다: **"이게 조용히 틀어질 수 있는 자리인가."**

지금까지 저지른 실수 전부는 `docs/이어받기.md` §5 에 있다. 읽어라.

---

## 2. 단 하나의 원칙

> **구조는 표본에서, 수준은 사용률에서.**

- **사용률**(`data/usage_single.json`) — 사다리 전체를 센 것. 숫자가 정확하다.
  대신 따로따로 알려 준다. "지진과 역린을 같이 쓰는 놈이 몇이냐" 는 모른다.
- **구축기사 표본**(`data/samples.json`) — 한 마리를 통째로 적은 것.
  조합을 알려 준다. 대신 수천 마리뿐이라 숫자는 안 정확하다.

**어떤 기술들이 뭉치는지는 표본에서, 각 기술을 얼마나 쓰는지는 사용률에서.**
지켜졌는지는 `mix_error` 로 잰다 — 어디서든 0.00%p 여야 한다.

표본은 **경향성을 확인하려고** 모으는 것이지 없으면 안 되는 것이 아니다.
표본이 없는 종은 기술을 서로 독립으로 뽑을 뿐, 채용률은 안 틀린다.

### 세기 전에 룰부터 맞춘다

사용률은 지금 룰(M-6), 구축기사는 대부분 지난 룰(M-5) 것이다.
**그 둘을 나란히 놓고 세면 안 된다.** 한 번 그렇게 해서
"상위 30종 중 7종에 표본이 없다, 구멍이다" 라고 틀리게 보고했다.
그 7종 중 6종은 M-6 에 새로 들어온 종이었다.
`samples.CURRENT_RULE` / `by_rule` / `newcomers` 를 쓴다.

---

## 3. 값을 정할 때

- **고원의 최댓값은 잡음이다.** 곡선이 평평한 구간에서 최댓값을 따라가지
  않는다. 값을 옮기지 말고 표본을 더 모은다. `samples.plateau()` 가
  고원 폭을 같이 보고한다. (한 번 1.5 → 0.75 로 내렸다가 되돌렸다.)
- **가정값은 `calc.py` CONFIG 한 곳에 모은다.** 어디서 온 값인지
  (감 / 표본으로 맞춤 / 사용자가 확인해 줌) 주석으로 같이 적는다.
- **격자 끝이 뽑히면 격자를 넓힌다.** 진짜 값이 바깥일 수 있다.

---

## 4. 작업 습관

- 검사는 `python3 tests.py`. **전부 통과해야 커밋한다.**
- **테스트가 깨졌을 때 테스트를 느슨하게 만들지 않는다.** 자료가 늘어서
  값이 움직인 것이면 그게 진짜 발견이다.
- 커밋 메시지는 한국어로, **무엇을 왜 바꿨는지와 숫자**를 적는다.
  틀렸다가 고친 것이면 틀린 과정도 적는다 — 그게 다음 사람에게 제일 쓸모 있다.
- 포켓몬 이름을 코드에 직접 쓸 때는 `dex.find_pokemon()` 으로 먼저 확인한다.
  **`페리퍼`가 아니라 `패리퍼`다.** 한 글자 틀리면 오류가 안 나고
  그 포켓몬이 조용히 없는 것이 된다.
- 백그라운드 실행에 `&` 를 붙이지 않는다 (두 번 작업이 죽었다).
- 수집기가 도는 중에 그 코드를 고치지 않는다 (옛 코드로 계속 돈다).

---

## 5. 도구 — 채우는 중

**설명문을 규칙으로 읽는다** (`battle.item_behaviors`). 166개 전부 읽힌다.

**읽어낸 것과 턴 루프가 실제로 쓰는 것은 다르다.** 그 경계가
`battle.APPLIED_ITEM_KINDS` 다. **코드를 붙이기 전에 그 집합에 넣지 마라.**
`Battle` 이 시작할 때 아직 안 붙은 것을 세서 경고에 넣는다 —
**승률 옆에 경고가 찍혀 있으면 그 숫자를 그대로 믿지 마라.**

아직 못 붙인 것: `quick`(선제공격손톱) · `drain_boost`(큰뿌리) ·
`pp`(과사열매) · `bind_chip`(조임밴드) · `metronome` · `only_for`(전기구슬) ·
`free_switch`(아름다운허물).

> **한 번 여기서 크게 틀렸다.** 처음에 구멍을 셀 때 `dex.item_effects`
> (데미지 표) 만 보고 "먹다남은음식·자뭉열매가 미구현" 이라고 보고했는데,
> 둘 다 턴 루프에 손으로 박혀 있었다. **표 하나만 보고 세면 또 틀린다.**

---

## 6. 게임 데이터에 숫자가 없는 것들

기술·도구 설명문이 **"OO 상태로 만든다" 까지만 적고 수치를 안 준다.**
그런 것은 본편 값을 가져다 쓰되 **`calc.CONFIG` 에 모으고 대전이 경고를
띄우게** 한다. 조용히 쓰면 나중에 실전과 어긋났을 때 어디를 의심해야
할지 모르게 된다.

지금 그렇게 가정하고 있는 것 —
압정 수치 · 스크린 감소율(1/2) · 날씨 타입 배율(1.5 / 0.5) ·
필드 타입 배율(1.3) · 모래 칩댐(1/16) · 상태이상 수치 전반.

실전에서 계산이 어긋나면 **여기부터 의심한다.**

---

## 7. 캐시 — 속도를 위해 외워 두는 것들

7단계(여러 턴 내다보기)는 대전을 수십만 번 돌린다. 그래서 몇 가지를
외워 둔다. **캐시는 조용히 틀어지기 제일 쉬운 자리다** — 열쇠에 하나만
빠져도 조건이 다른데 같은 답을 돌려주고, 터지지 않는다.

- `best.rate_moves` — 열쇠는 `calc_damage` 가 **읽는 필드 전부**여야 한다
  (ability · hp_ratio · item · ranks · 종족값/노력치/성격 · status · types).
- `best.CHECK_CACHE = True` 로 켜면 캐시를 믿지 않고 **매번 다시 계산해서
  저장된 답과 대조**하고, 다르면 터진다. `[40]` 이 이 모드로 120판 돌린다.
  **캐시에 무엇을 더하든 그 대조 모드를 같이 만든다.**
- `id(객체)` 를 열쇠로 쓰지 않는다. 파이썬은 객체가 사라지면 그 id 를
  다시 내준다 — 새 Dex 가 옛 Dex 의 표를 물려받는다. dex 에 직접 붙인다.

지금 속도: 3대3 한 판 **8.6ms**(초당 117판), 1대1 **2.0ms**(초당 511판).
