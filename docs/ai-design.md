# AI 설계 방향

이 문서는 **"최선의 수를 어떻게 판단할 것인가"** 와 **"그 AI를 어떻게 스스로 강해지게 만들 것인가"** 를 정리한 것이다.

새 대화에서 이어서 작업할 때 이 문서부터 읽으면 된다.

---

## 1. 지금 어디까지 왔나

| 단계 | 상태 |
|---|---|
| 1. 게임 데이터 확보 | 완료 |
| 2. 실전 사용률 데이터 | 완료 (시즌 6) |
| 3. 데미지 계산기 | 완료 (`calc.py`) |
| 4. 한 턴 최선수 추천 | **다음 차례** |
| 5. 여러 턴 탐색 AI | 아직 |
| 6. 자가발전 | 아직 |

---

## 2. 계산기가 지금 반영하는 것 / 못 하는 것

점검한 결과를 그대로 적는다. **"대충 된다"고 넘기면 나중에 틀린 걸 최적화하게 된다.**

### 능력치·데미지

| 항목 | 상태 |
|---|---|
| 레벨 50 실능 계산 (종족값·노력치·성격) | 반영 — 기존 공식과 33,660회 대조, 불일치 0 |
| 랭크 변화 (+6 ~ −6) | 반영 |
| 타입 상성 (4배 약점 포함) | 반영 — 직접 검증한 챔피언스 표 사용 |
| 자속 보정 | **반영** (배율 1.5는 미확인 → 아래 4장) |
| 급소 | **반영** (배율 1.5는 미확인) |
| 데미지 난수 16단계 | 반영 |
| 화상 시 물리 반감 | 반영 (배율 0.5는 미확인) |

### 특성 (전체 217개 중 데미지 관련 110개)

| 구분 | 개수 |
|---|---|
| 계산에 반영됨 | **42** |
| 반영은 안 되지만 경고로 알려줌 | 17 |
| 아직 아무 처리 없음 | 51 |

반영된 것 중 중요한 것:

- **무효화** — 부유, 천정부지, 축전, 피뢰침, 저수, 건조피부, 초식, 방음, 방탄
  (이건 배율이 틀리는 게 아니라 **답이 통째로 틀리는** 종류라 최우선으로 넣었다)
- **타입 변환** — 스카이스킨, 프리즈스킨, 페어리스킨, 드래곤스킨
- **기술 분류 기반** — 철주먹(펀치), 예리함(베기), 메가런처(파동), 옹골찬턱(무는), 펑크록(소리)
- **위력/능력치 배율** — 천하장사, 순수한힘, 의욕, 근성, 적응력, 테크니션, 단단한발톱,
  스나이퍼, 심록·맹화·급류·벌레의알림, 불꽃의갈기, 강철정신, 페어리오라, 수포
- **방어측** — 두꺼운지방, 내열, 건조피부, 정화의소금, 필터, 하드록, 파동의방호,
  복슬복슬, 퍼코트, 멀티스케일

아직 못 하는 것 중 **메타 상위권에 실제로 있는 것**:

| 포켓몬 | 특성 | 왜 아직 못 하나 |
|---|---|---|
| 따라큐 (11위) | 탈 | 첫 공격을 통째로 무효 — 데미지 배율이 아니라 별도 규칙 |
| 브리두라스 (8위) | 지구력 | 맞을 때마다 방어 상승 — 여러 턴에 걸친 변화 |
| 한카리아스 (2위) | 까칠한피부 | 공격한 쪽이 반동 — 상대 HP를 깎는 별개 계산 |
| 드닐레이브 (7위) | 열교환 | 불꽃 맞으면 공격 상승 |

이것들은 **계산 결과에 `!` 경고로 표시**되므로 모르고 지나칠 일은 없다.

### 기술

공격기 335개 중 **256개(76%)는 위력 그대로** 들어가서 문제없다. 나머지 79개는 특수 규칙이 있다.

| 종류 | 개수 | 예 |
|---|---|---|
| 날씨 의존 | 17 | 눈보라, 솔라빔 |
| 반동 | 16 | 이판사판태클, 파괴광선 |
| 필드 의존 | 16 | 지진(그래스필드), 사이코팽 |
| 연속 히트 | 14 | 바늘미사일, 고드름침 |
| 위력 가변 | 10 | 자이로볼, 바둥바둥 |
| 고정 데미지 | 6 | 나이트헤드, 지구던지기 |

### 아예 없는 것

날씨 · 필드 · 리플렉터/빛의장막 · 압정/스텔스록 · 교체 · 턴 진행

**즉 지금은 "한 대 때렸을 때 얼마 들어가나"까지만 된다.** 대전 자체를 돌릴 수는 없다.

---

## 3. 최선의 수를 어떻게 판단할 것인가

### 3-1. 데미지 비교만으로는 안 되는 이유

기술 4개의 데미지를 비교하면 변화기는 전부 0이라 영원히 안 뽑힌다.
그런데 실전에서는 칼춤이 정답인 경우가 훨씬 많다.

원인은 **"이번 턴의 이득"만 보기 때문**이다. 칼춤은 이번 턴 이득이 0이고 다음 턴부터 이득이 난다.

### 3-2. 해결 — 모든 수를 같은 자로 잰다

비교하려면 단위가 같아야 한다. 그 단위는 결국 하나다.

> **"이 수를 두면 내가 이길 확률이 얼마나 올라가는가"**

직접 계산할 수 없으니, **몇 턴 뒤의 상황에 점수를 매기고 거슬러 올라오는** 방식을 쓴다.
체스·바둑 AI와 같은 원리다.

### 3-3. 기술 종류별 환산

| 종류 | 어떻게 점수로 바꾸나 |
|---|---|
| 공격기 | 준 데미지 |
| 랭크업기 | 이번 턴 0, 다음 턴부터 데미지 배수. 그 사이 맞을 데미지가 비용 |
| 회복기 | 회복한 HP = 마이너스 데미지 |
| 상태이상기 | 앞으로 **받을** 데미지가 줄어드는 양 (화상=물리 반감, 마비=스피드 절반) |
| 압정·스텔스록 | 상대가 교체할 때마다 누적 → 남은 턴이 길수록 가치가 큼 |
| 교체 | 이번 턴 공짜로 맞아주는 비용 vs 상성 우위 이득 |

칼춤이 좋은지는 **"2턴 뒤에 내가 더 유리한가"** 로 판가름난다.
상대가 나를 2방에 잡으면 칼춤은 자살, 3방 이상 걸리면 이득. **이건 계산으로 나온다.**

### 3-4. 포켓몬이 체스보다 어려운 두 가지

**(1) 동시에 수를 낸다**

체스는 번갈아 두지만 포켓몬은 서로 모르는 채 동시에 고른다.
그래서 "최선의 수 하나"가 존재하지 않는 상황이 생긴다.
상대가 교체할 것 같으면 칼춤, 공격할 것 같으면 나도 공격 — 그런데 상대도 똑같이 생각한다.

이건 **확률을 섞어서 내는 것이 정답**인 문제다(가위바위보와 같음). 수학적으로 푸는 방법이 있다.

**(2) 상대의 정보를 모른다 — 여기서 로토덱스가 결정적이다**

상대가 어떤 기술·도구·노력치를 들고 있는지 모른다. 하지만 사용률이 있다.

```
한카리아스  지진 67%  스텔스록 39.6%  용성군 30.1%
            한카리아스나이트Z 35.3%  구애스카프 19%
            성격 명랑 24.2%  고집 21%
```

**"상대가 지진을 들고 있을 확률 67%"** 로 계산할 수 있다.
시중 계산기에는 없는 재료이고, 이 프로젝트의 핵심 무기다.

대전이 진행되면 정보가 늘어난다. 지진을 한 번 쓰면 100%로 확정되고,
예상보다 먼저 움직이면 스카프를 들었다는 뜻이다.
**관찰로 확률을 좁혀가는 것**까지가 완성형이다.

### 3-5. 진행 순서

| 단계 | 내용 | 난이도 |
|---|---|---|
| **4-A** | 기술 4개 데미지 + **스피드 비교** ("내가 먼저 때리는가") | 쉬움 |
| **4-B** | 변화기 환산 규칙 (랭크업·회복·상태이상) | 보통 |
| **4-C** | 상대 기술을 확률로 다루기 (사용률 활용) | 보통 |
| **5** | 2~3턴 앞 탐색 + 동시 수 처리 | 어려움 |

**4-A가 생각보다 크다.** "내가 먼저 때려서 한 방에 잡는다"만 알아도 실전 판단의 절반은 끝난다.
스피드는 이미 데이터에 있으니 바로 된다.

---

## 4. ⚠️ 그 전에 반드시 확인해야 할 것

지금 계산기에는 **확인 못 한 가정이 세 개** 있다. (`calc.py` 의 `CONFIG`)

| 항목 | 가정값 | 확인 방법 |
|---|---|---|
| 자속 보정 | 1.5배 | 실제 대전 데미지와 대조 |
| 급소 배율 | 1.5배 | 급소 떴을 때 데미지와 대조 |
| 개체값 체계 | 없어지고 최대 고정 | 게임 화면의 능력치 한 마리 |

**포켓몬 한 마리의 화면 능력치 6개 + 성격 + 노력치 배분**만 알려주면 세 개 다 확정된다.

### 왜 이걸 먼저 해야 하나

> **규칙이 틀린 시뮬레이터에서 학습하면, 틀린 것에 최적화된 AI가 나온다.**

그리고 **틀렸다는 걸 눈치채기 어렵다.** 시뮬레이터 안에서는 승률이 계속 오르기 때문이다.
나중에 고치면 그동안 학습한 걸 전부 버려야 할 수도 있다.

---

## 5. 자가발전 계획

### 전제 조건 딱 하나

> **AI가 스스로 대전을 해볼 수 있어야 한다.**

| | 하루에 둘 수 있는 판수 |
|---|---|
| 실제 게임 | 100판 정도 |
| 시뮬레이터 | 수십만 판 |

**시뮬레이터의 완성도가 자가발전 속도의 상한선이다.** 여기가 부실하면 위에 뭘 올려도 소용없다.

### 방법 (쉬운 순서)

**A. 자기대전 + 통계 축적** — 지금 데이터로도 가능

같은 상황에서 A안과 B안을 각각 1만 번씩 돌려 승률을 비교한다.

> "한카리아스가 하마돈 앞에서 칼춤 vs 지진 → 칼춤 승률 58%, 지진 52%"

학습이라기보단 **실험**이다. 대신 확실하고 **왜 그런지 설명이 된다.**

**B. 평가 기준 자동 조정** — 가성비가 제일 좋음

AI가 상황에 점수를 매길 때 쓰는 숫자들:

```
내 HP 1% 의 가치        = ?
공격 랭크 1단계의 가치   = ?
상대를 마비시킨 가치     = ?
스텔스록 깔아둔 가치     = ?
```

처음엔 사람이 감으로 정하고, **숫자를 조금씩 바꾼 여러 AI를 서로 붙여 이긴 쪽을 남기고 변형**시킨다.
수백 세대면 사람이 정한 값보다 훨씬 나아진다. 개인 PC로 충분하다.

**C. 탐색 깊이 늘리기** — 계산 시간이 곧 실력

2턴 → 4턴 → 6턴. 엄밀히는 자가발전이 아니라 "더 오래 고민하기"지만 효과는 확실하다.

**D. 강화학습 자기대전** — 가장 강하지만 가장 어려움

알파고 방식. 다만 포켓몬은 **상대 패를 모르고 동시에 수를 내서** 바둑보다 까다롭다.
오히려 포커 AI가 쓰는 방법(후회 최소화 계열)이 더 맞을 수 있다. 계산 자원이 많이 든다.

**A와 B까지가 현실적인 목표이고, 그것만으로도 충분히 강해진다.**

### "결국 최선의 수에 도달하는가"

- **이론적으로** 도달점은 존재한다 (경우의 수가 유한하므로)
- **현실적으로** 완전 해결은 불가능하다 (경우의 수가 천문학적)
- **하지만 "사람이 이길 수 없는 수준"에는 도달 가능하다**

### 중요한 구분

| | 뜻 | 특징 |
|---|---|---|
| 균형 전략 | 상대도 완벽하다고 가정한 최선 | 크게 지지 않음. 대신 약한 상대도 크게 못 이김 |
| **착취 전략** | 실제 사람들의 경향을 파고드는 최선 | 훨씬 크게 이김. 대신 예상 밖 상대에게 당함 |

**이 프로젝트는 두 번째에 유리하다.** 로토덱스 사용률이 곧 "사람들이 실제로 뭘 하는지"의 기록이기 때문이다.

### 자가발전의 두 가지 함정

1. **틀린 규칙을 최적화** — 4장의 세 가정을 먼저 확정할 것
2. **목표를 잘못 정의** — "HP를 높게 유지하라"고만 가르치면 공격을 안 하고 회복만 하다 시간패한다

---

## 6. 다음에 할 일

1. **게임 화면에서 포켓몬 한 마리의 능력치 확인** → 4장의 가정 세 개 확정
2. **4-A 구현** — 스피드 비교 + 기술 4개 일괄 비교
3. 4-B, 4-C
4. 턴 진행 시뮬레이터 (5단계와 자가발전의 전제)

---

## 참고 — 코드 위치

| 파일 | 내용 |
|---|---|
| `calc.py` `CONFIG` | 미확인 가정값 3개. 여기 숫자만 고치면 전체에 반영됨 |
| `calc.py` `ATTACKER_ABILITY` 등 | 특성 표. 새 특성을 넣으려면 여기 추가 |
| `data/moves.json` `tags` | 기술 분류(펀치·베기·파동·무는·소리 등). 특성 판정에 씀 |
| `tests.py` | 고친 뒤 반드시 실행. 22개 항목 검사 |
